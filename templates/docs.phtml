<?= $this->fetch('includes/docs-header.phtml') ?>

<section class="site-body">
    <div class="container">
        <div class="docs">
            <div class="doc__side">
                <?php foreach ($docs as $group): ?>
                    <h3 class="doc__side-title"><a href="/docs/<?= $group->getName() ?>"><?= $this->fetch('includes/icon.phtml', ['name' => "home"]) ?> <?= $group->getTitle(); ?></a></h3>
                    <ul class="doc__side-list">
                        <?php foreach ($group as $docSection): ?>
                            <li class="doc__side-item">

                                <?php if ($this->exists($docSection->getTemplateFile())) : ?>
                                    <a class="doc__side-link active"
                                        href="<?= $docSection->getHref() ?>">
                                        <?= $docSection->getTitle(); ?>
                                    </a>
                                <?php else: ?>
                                    <span class="doc__side-link"
                                        href="<?= $docSection->getHref() ?>">
                                        <?= $docSection->getTitle(); ?>
                                    </span>
                                <?php endif; ?>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                <?php endforeach; ?>
                <h3 class="doc__side-title">API</h3>
                <ul class="doc__side-list">
                    <li class="doc__side-item">
                        <span class="doc__side-link"
                            href="/api-docs">API Reference
                        </span>
                    </li>
                </ul>
            </div>
            <div class="doc__content">
                <?= $content; ?>

                <div class="doc__controls">
                    <div class="doc__previous">
                        <?php if ($doc->getPrev()) : ?>
                            <a class="" href="<?= $doc->getPrev()->getHref() ?>">⇠ <?= $doc->getPrev()->getTitle() ?></a>
                        <?php endif; ?>
                    </div>

                    <div class="doc__home">
                        <?php if ($doc->getHome()) : ?>
                            <a href="<?= $doc->getHome()->getHref() ?>"><?= $this->fetch('includes/icon.phtml', ['name' => "home"]) ?></a>
                        <?php endif; ?>
                    </div>

                    <div class="doc__next">
                        <?php if ($doc->getNext()) : ?>
                            <a class="" href="<?= $doc->getNext()->getHref() ?>"><?= $doc->getNext()->getTitle() ?> ⇢</a>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
